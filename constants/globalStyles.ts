import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  containerWithPadding: {
    flex: 1,
    padding: 20,
  },
  scrollContent: {
    padding: 20,
  },
  errorText: {
    marginBottom: 10,
    backgroundColor: '#FFF',
    padding: 10,
    borderRadius: 8,
  },
});
